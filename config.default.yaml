# kvmd-serial-term default configuration
# Copy to /etc/kvmd/serial-term.yaml and edit as needed

serial:
  device: /dev/ttyUSB0
  baudrate: 115200

server:
  unix_socket: /run/kvmd/serial-term.sock
  web_dir: /usr/share/kvmd/web/kvmd-serial-term
